
public class statickeyword2 {
    public static void main(String[] args) {
        // Static=Makes a variable or method belong to class 
        //        rather than to any specific object
        //        Commonly used for utility methods or shared resources.

        // Another type wehere we can see static method is math class 
        // This is the continuation of statickeyword.java

        Math.round(3.99);
        // Round is a utility method we cam accesss it thr the math class
        // we dont nedd tp create a math object such as Math math=new Math();
        // That will be silly
        // round is a static method it  behaves as an utility method and we dont need to crate an object to call it
        // beacuse thats stupid?
        // round is a utility method for Math class
        // we can access it thr the Math class

        // thats static keyword it modifies  a variable or method to class 
        // rather than a specific object
        // commonly used for utility methos or shared resources among objects created for the class
    }
}
