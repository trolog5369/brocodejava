# 📘 Day 07 — Substrings & Dynamic String Manipulation

## 🧠 Concepts Covered
1. **`substring()` Method** — Extracting specific portions of a string.  
2. **`indexOf()` Method** — Locating character positions to make string extraction dynamic.  
3. **Combining `substring()` and `indexOf()`** — Creating flexible and reusable string logic.  
4. **User Input Handling** — Using `Scanner` to accept email addresses dynamically.  
5. **Validation with Conditionals** — Checking for valid email formats using `contains()`.

---

## 🧩 Mini Programs
- **`substring.java`** — Extracts username and domain from an email using hardcoded indices.  
- **`substring2.java`** — Automates extraction using `indexOf()` to find `"@"` dynamically.  
- **`substringuserinput.java`** — Accepts user input and prints extracted username and domain.  
- **`substringuserinput2.java`** — Adds validation to ensure the input email contains `"@"`.

---

## 🧠 Concepts Reinforced
- String slicing and character indexing  
- Dynamic data handling  
- Input validation  
- Code reusability through flexible logic

---

## 🧩 Sample Output
Enter your email: pranavgaikwad847@gmail.com

pranavgaikwad847
gmail.com


---

## 🪞 Reflection
Today’s lesson showed how **small methods like `substring()` and `indexOf()` unlock powerful string processing capabilities**.  
These techniques are crucial for data parsing, form validation, and text-based logic in larger applications.

