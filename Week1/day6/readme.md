## 🚀 Day 6 — Java Fundamentals Progress  

### 📘 Topics Covered:
- **String Methods**
- **Compound Interest Calculator**
- **Nested If & Nested If-Else Statements**

---

### 💻 Programs Practiced:
#### 🧩 `stringmethods.java`
- Explored core **String methods** such as:
  - `length()`, `charAt()`, `indexOf()`, `lastIndexOf()`
  - `toUpperCase()`, `toLowerCase()`, `trim()`, `replace()`
  - `isEmpty()`, `contains()`, `equals()`, `equalsIgnoreCase()`
- Learned how to handle **case sensitivity** in string comparisons.
- Practiced validating user inputs using string methods.

#### 💰 `compoundintrestcal.java`
- Built a **Compound Interest Calculator** using:
  - User input via `Scanner`
  - Formula: `A = P * (1 + R/N) ^ (N*T)`
  - Output formatting with `printf` for clean decimal precision.
- Strengthened understanding of **Math class** and **formatted output**.

#### 🎯 `nestedifstatements.java` & `nestedifstatements2.java`
- Implemented **conditional logic** for discount systems.
- Understood difference between **simple if-else** and **nested if-else**.
- Applied **boolean flags** to simulate multiple condition checks (student + senior).
- Learned clean, readable condition handling with nested logic.

---

### 🧠 Key Learnings:
- How **string manipulation** works internally in Java.
- Importance of **conditional flow** in real-life decision-making logic.
- Use of **format specifiers** and **input handling** for clean outputs.

---

### ⚔️ Next Focus (Day 7 Preview):
- Practice with **`switch` statements** and **logical operators (`&&`, `||`, `!`)**
- Mini challenges using today’s concepts:
  - Username generator using `substring()`
  - Simple vs Compound Interest selector
  - Multi-condition discount logic (add coupon system)

