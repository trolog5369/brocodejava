
// This is the superclass(parent)
// This is a abstracted class hence we gottan deefine the methods in this class in the subclasses respectively
public abstract class Vechile {
    abstract void go();
    // This is a abstrcated method
}

// For interface just use this and then in the subclass use implements instead of extends
// public interface Vechile {
//      void go();
//     // This is a abstrcated method by default
// }